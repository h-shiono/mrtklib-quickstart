# Keep shell scripts LF even on Windows checkouts, so the WSL bash that runs
# them (e.g. detect-sbf-port.sh invoked from run-container.ps1) does not choke
# on CR characters.
*.sh text eol=lf

# Windows-native scripts stay CRLF.
*.bat text eol=crlf
*.ps1 text eol=crlf
